重要紧急知识点与非紧急知识点：
*****
	cookie
	 	存储数据
	 		a).不能跨域
	 		b).容量小:4KB
	 		c).不安全
	 		d).名字是唯一
	 		e).有生命周期:默认是session
	 		f).不能跨浏览器
	requireJS\seaJS
		模块化
			a).解决变量名冲突
			b).降低代码耦合
			c).不用手动引入很多js
			d).解决js之间的依赖关系
	JQuery
		库 	帮助我们快速开发
		强大之处？
			a).选择器
			b).扩展性

	正则
		a).操作字符串
		b).表单校验

	交互
		ajax
		jsonp
	面向对象
	        AngularJS

***
	语言特性+性能优化
	ZQuery
	php+mysql+nodejs
	高级运动
	算法+数据结构